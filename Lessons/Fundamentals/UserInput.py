# input() allows user to input data, result is stored inside variable
name = input("Enter your name: ")

# Can type cast user's input to a specific data type
# User is entering a whole number, therefore type cast it to an integer
age = int(input("Enter your age: "))

# Print out name and age that user input
print(f"Hello {name}, you are {age} years old.")